/*
 * -------------------------------------------------
 *  Nextflow config file for running tests
 * -------------------------------------------------
 * Defines bundled input files and everything required
 * to run a fast and simple test. Use as follows:
 *   nextflow run nf-core/airrflow -profile test_10x_sc,<docker/singularity>
 */

params {
    config_profile_name = 'Test 10xGenomics single cell data'
    config_profile_description = 'Minimal test dataset to check pipeline function with raw single cell data from 10xGenomics'

    // Limit resources so that this can run on GitHub Actions
    max_cpus = 2
    max_memory = 6.GB
    max_time = 48.h

    // params
    mode = 'fastq'
    sc_raw = true
    library_generation_method = 'sc_10x_genomics'
    clonal_threshold = 0


    // Input data
    input = '/home-link/kymmp01/workdir/pipeline_dev/cellranger2airrflow/airrflow/conf/10x_sc_raw.tsv'
    reference_10x = '/home-link/kymmp01/workdir/pipeline_dev/cellranger2airrflow/cellranger_ref/vdj_IMGT_human.tar.gz'
}
